---
{{ include "bjw-s.common.loader.init" . }}

{{ $_ := mergeOverwrite .Values (include "tigerbeetle.hardcodedValues" . | fromYaml) }}

{{/* Render the templates */}}
{{ include "bjw-s.common.loader.generate" . }}
